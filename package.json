{
  "private": true,
  "name": "carcer",
  "workspaces": ["packages/core", "packages/shell"],
  "scripts": {
    "build-brain": "cd brain && yarn build",
    "build-cocoa": "cd examples/cocoa && yarn build",
    "build-docker-node": "cd examples/node && yarn build-docker",
    "start-cocoa": "cd examples/cocoa && yarn start",
    "start-node": "cd examples/node && yarn start",
    "start-docker-node": "cd examples/node && yarn start-docker"
  }
}
